torch_dataset_name: vel_dataset 

train:
  max_epochs: 500 
  batch_size: 2
  shuffle_data: True
  time_window: 3

model:
  model_name: unet

optimizer:
  initial_lr: 1e-3

lr_scheduler:
  factor: 0.5
  patience: 100
  min_lr: 1e-8
